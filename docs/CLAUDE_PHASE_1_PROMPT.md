Read `CLAUDE.md`, `docs/PHASE_1_BRIEF.md`, and the two approved product documents in `../deliverables` completely before editing.

Implement Phase 1 end to end in the current directory. This is an implementation task, not a planning-only task.

Requirements:

- Scaffold the Next.js App Router + TypeScript project in this directory.
- Implement the approved role-aware interface and first vertical slice from the brief.
- Use only synthetic mock data behind typed adapter interfaces.
- Preserve every non-negotiable product rule from `CLAUDE.md`.
- Install only justified dependencies.
- Add automated tests for the workflow order, role visibility, customer isolation, and tooltip behavior where practical.
- Add a concise README.
- Run formatting/linting, tests, and a production build; fix failures.
- Do not edit files outside the current project directory.
- Do not use real credentials or personal data.
- Do not stop after scaffolding: deliver a coherent, visually polished reviewable application.

Use good judgment on details that are not specified. At the end, report what was built, which checks passed, and what intentionally remains mocked.

