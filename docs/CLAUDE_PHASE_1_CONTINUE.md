The previous response was interrupted by an API connection closure after partial implementation. Continue from the current filesystem state and complete Phase 1. Do not restart or discard working files.

Inspect what is already present, then finish all required screens, shared components, styling, role behavior, mock adapters, tests, and README from `CLAUDE.md` and `docs/PHASE_1_BRIEF.md`.

Run lint, tests, and the production build. Fix all failures. Deliver a coherent reviewable application, not a partial scaffold. Do not edit anything outside the current project directory.

